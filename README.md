# solo-releases

A local AI coding agent for the desktop. Solo runs entirely on your machine — no cloud,
no API keys, no telemetry — and brings its own inference engine, so there is nothing to
install alongside it. It is a senior **C++ and Lua** engineer by default, but works in
any language.
